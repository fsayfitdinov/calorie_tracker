import 'package:flutter/material.dart';

class MacronutrientsPage extends StatelessWidget {
  const MacronutrientsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
